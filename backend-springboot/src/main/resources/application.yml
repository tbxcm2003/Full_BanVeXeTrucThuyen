spring:
  application:
    name: quan-ly-tai-khoan
  datasource:
    # Trùng database/data.sql: CREATE DATABASE QuanLyVeXe — import toàn bộ file (schema + mẫu) trước khi chạy; ddl-auto: validate
    # Nếu máy bạn dùng CSDL tên cũ (ví dụ quanlyveXe), hoặc đổi tên DB trong MariaDB, hoặc sửa lại tên ở đây cho khớp
    url: jdbc:mariadb://localhost:3306/QuanLyVeXe?characterEncoding=utf8&useUnicode=true
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: sapassword
  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        default_batch_fetch_size: 32
        jdbc:
          batch_size: 32
  mail:
    host: smtp.gmail.com
    port: 587
    username: support.banvexe@gmail.com
    password: iysfirkwxodmxpwi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

app:
  jwt:
    secret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    expiration-minutes: 1440
  otp:
    expiration-minutes: 5
  # Bật true khi dev: API 500 trả thêm nội dung lỗi thật (NPE, SQL, …). Tắt (false) trên môi trường production.
  expose-internal-error-message: true
